apiVersion: v1
kind: Service
metadata:
    name: tavaresvieiraconversaotemperatura
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: tavaresvieiraconversaotemperatura